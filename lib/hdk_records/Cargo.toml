[package]
name = "hdk_records"
version = "0.1.0"
authors = ["pospi <pospi@spadgos.com>"]
edition = "2018"

[dependencies]
thiserror = "1.0"
serde = "1"
# :DUPE: hdk-rust-revid
hdk = "0.0.100"
holo_hash = "0.0.2-alpha.1"

serde_maybe_undefined = { path = "../serde_maybe_undefined" }
hdk_type_serialization_macros = { path = "../hdk_type_serialization_macros" }

[lib]
crate-type = ["lib"]
