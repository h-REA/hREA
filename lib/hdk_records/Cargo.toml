[package]
name = "hdk_records"
version = "0.1.0"
authors = ["pospi <pospi@spadgos.com>"]
edition = "2018"

[dependencies]
thiserror = "1.0"
serde = "1"
# :DUPE: hdk-rust-revid
hdk = "0.0.100"
# :TODO: split newtype macro and base types out of vf_core
vf_core = { path = "../vf_core" }

[lib]
crate-type = ["lib"]
